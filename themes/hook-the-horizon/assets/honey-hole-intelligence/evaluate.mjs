import{rejectSensitiveLocation,sanitizePublicPayload}from'./privacy.mjs';
const FRESHNESS_DAYS=Object.freeze({closure:1,hazard:1,weather:1,waterCondition:1,regulation:7,permit:7,access:30,ownership:30,conservation:90});
const FIELDS=Object.keys(FRESHNESS_DAYS);
export function evaluateHoneyHole(input,now=new Date()){const errors=[];if(!input?.publicRegion)errors.push('publicRegion is required');if(!input?.tripType)errors.push('tripType is required');if(!input?.facts||typeof input.facts!=='object')errors.push('facts are required');if(errors.length)return{status:'invalid',errors};const locationCheck=rejectSensitiveLocation({publicRegion:input.publicRegion,notes:input.notes||null});if(!locationCheck.ok)return{status:'sensitive_location_rejected',applicationId:'HHI-001',findings:locationCheck.findings,message:'Exact or inference-enabling location information is not accepted. Use a broad public region and current official sources.'};const unknownFields=[];const staleFields=[];const contradictions=[];const missingOfficialSources=[];for(const key of FIELDS){const fact=input.facts[key];if(!fact||fact.value===null||fact.value===undefined||['unknown','unverified'].includes(fact.status))unknownFields.push(key);if(isStale(key,fact,now))staleFields.push(key);if(fact?.status==='contradicted')contradictions.push(key);if(!fact?.sourceUrl||!fact?.verifiedAt)missingOfficialSources.push(key)}const hardStops=[];if(input.facts.closure?.value==='closed')hardStops.push('An official closure is recorded.');if(input.facts.access?.value==='not_permitted')hardStops.push('Legal access is not supported.');if(input.facts.hazard?.value==='severe')hardStops.push('A severe hazard is recorded.');if(input.facts.conservation?.value==='avoid_pressure')hardStops.push('Current conservation controls indicate avoiding additional pressure.');if(contradictions.includes('closure')||contradictions.includes('access')||contradictions.includes('regulation'))hardStops.push('A material official-source contradiction affects legality or access.');const criticalUnknowns=['closure','hazard','regulation','access'].filter(key=>unknownFields.includes(key)||staleFields.includes(key));let decisionState='ready_with_controls';if(hardStops.length)decisionState='do_not_proceed';else if(criticalUnknowns.length||contradictions.length)decisionState='verify_before_trip';else if(!input.gearReady||!input.backupPlanReady)decisionState='preparation_incomplete';const officialQuestions=unique([...(unknownFields.includes('regulation')||staleFields.includes('regulation')?['What regulations and seasonal restrictions apply today?']:[]),...(unknownFields.includes('closure')||staleFields.includes('closure')?['Are there active closures, emergency orders, or temporary restrictions?']:[]),...(unknownFields.includes('access')||staleFields.includes('access')?['Is the intended public access route legal and currently open?']:[]),...(unknownFields.includes('hazard')||staleFields.includes('hazard')?['What current weather, water, fire, road, and field hazards apply?']:[]),...(unknownFields.includes('conservation')||staleFields.includes('conservation')?['What conservation, species, handling, or pressure-reduction controls apply?']:[])]);const publicResult={applicationId:'HHI-001',publicRegion:input.publicRegion,tripType:input.tripType,decisionState,hardStops,criticalUnknowns,unknownFields,staleFields,contradictionFields:contradictions,missingOfficialSourceFields:missingOfficialSources,officialQuestions,readiness:{gearReady:Boolean(input.gearReady),backupPlanReady:Boolean(input.backupPlanReady)},backupPlan:input.backupPlanReady?'Use the prepared public-safe alternative if any closure, hazard, access, or conservation condition changes.':'Create a public-safe alternative that does not depend on the same access, conditions, or resource pressure.',boundary:'This is public-region trip preparation, not a guarantee of legality, access, conditions, safety, or catch. Verify critical facts with the current official authority before departure.'};const sanitized=sanitizePublicPayload(publicResult);return{status:sanitized.ok?'trip_readiness':'trip_readiness_redacted',...sanitized.sanitized,privacyFindings:sanitized.findings,evidence:FIELDS.map(key=>summarize(key,input.facts[key],now))}}
function summarize(key,fact,now){return{key,value:fact?.value??null,status:fact?.status||'unverified',verifiedAt:fact?.verifiedAt||null,sourceUrl:fact?.sourceUrl||null,stale:isStale(key,fact,now)}}
function isStale(key,fact,now){if(!fact)return false;if(fact.status==='official_stale')return true;const verifiedAt=Date.parse(fact.verifiedAt||'');return Number.isFinite(verifiedAt)&&now.getTime()-verifiedAt>FRESHNESS_DAYS[key]*86_400_000}
function unique(values){return[...new Set(values)]}
